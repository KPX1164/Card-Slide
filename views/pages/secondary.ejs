<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partials/head'); %>
  </head>
  <body class="font-primary bg-prevent">
    <section class="font-primary w-full VStack items-center">
      <!-- site04.ejs -->
      <div class="w-screen flex flex-col items-center">
        <div class="w-4/5">
          <div class="VStack">
            <% data['Main page'].forEach((data) => { %>
                <%- include('../partials/card', { data: data }) %>
            <% }); %>
          </div>
        </div>
      </div>
    </section>
    <footer><%- include('../partials/footer'); %></footer>
    <style>
      .bg-prevent {
        background-image: url('images/site01.jpg');
        background-size: cover;
        /* background-position: center; */
      }
    </style>
  </body>
</html>

